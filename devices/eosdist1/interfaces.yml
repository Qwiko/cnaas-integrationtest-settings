---
interfaces:
  - name: Ethernet1
    ifclass: custom
    config: |-
      no switchport
      ip address 10.100.2.101/24
      no lldp transmit
      no lldp receive
  - name: Ethernet2
    ifclass: downlink
  - name: Vlan1
    ifclass: custom
    config: |-
      ip address 192.168.0.1/24
      ip helper-address 10.100.2.2
      no shutdown

